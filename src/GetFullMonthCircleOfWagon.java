public class GetFullMonthCircleOfWagon {
/*

    HashMap<String, Double> map = new HashMap<>();

    public void fullDays(String numberOfWagon, String distanceOfEmpty, String distanceOfRoute, HashMap<Integer, String> tempMapOfWagons) {
        int countWagons = tempMapOfWagons.size();

        for (int i = 0; i < countWagons; i++) {
            String[] valueOfTempMapOfWagons = tempMapOfWagons.get(i).split(", ");

            String numberOfWagonFullCircle = valueOfTempMapOfWagons[0].trim();
            if (numberOfWagonFullCircle.equals(numberOfWagon)) {
                fullMonthCircle += Double.valueOf(distanceOfEmpty) / 300 + 1;
                fullMonthCircle += LOADING_OF_WAGON;
                fullMonthCircle += Double.valueOf(distanceOfRoute) / 300 + 1;
                fullMonthCircle += UNLOADING_OF_WAGON;
                System.out.println(numberOfWagonFullCircle + ": " + fullMonthCircle);
            }
        }
    }*/
}
